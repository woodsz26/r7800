Index: drivers/nvmem/core.c
===================================================================
--- drivers/nvmem/core.c	(revision 32783)
+++ drivers/nvmem/core.c	(revision 33429)
@@ -114,5 +114,5 @@
 	rc = nvmem_reg_read(nvmem, pos, buf, count);
 
-	if (rc)
+	if (rc < 0)
 		return rc;
 
@@ -148,5 +148,5 @@
 	rc = nvmem_reg_write(nvmem, pos, buf, count);
 
-	if (rc)
+	if (rc < 0)
 		return rc;
 
